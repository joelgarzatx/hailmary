extends layout

block content
  h1= title
  p Welcome to #{title}
  
  p Recently added: #{user}

  div.namebox
      form(name="add", action="/add", method="post")
        input(type="text", name="user")
        input(type="submit", value="submit")
